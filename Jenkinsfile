pipeline {
            agent any
            stages {
                stage('Checkout') {
                    steps {
                        git url: 'https://github.com/neha23031991/GitTrainingPurposeTest.git', branch: 'main'
                    }
                }
                // ... other stages (build, test, deploy)
            }
        }
