name: CI

on:
  push:
    branches: [ "GitActionBranch" ]
  pull_request:
    branches: [ "GitActionBranch" ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # pick one setup block that matches your stack
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install
        run: npm ci

      - name: Test
        run: npm test
